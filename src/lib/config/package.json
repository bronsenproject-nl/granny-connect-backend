{
  "name": "grannyconnect-config",
  "version": "0.0.1",
  "description": "GrannyConnect config helper, precedes arguments over environment over defaults",
  "main": "index.js",
  "// comment": "npm version major|minor|patch",
  "scripts": {
    "test": "JASMINE_CONFIG_PATH=tests/support/jasmine.json node_modules/jasmine/bin/jasmine.js",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "jasmine": "^2.4.1"
  },
  "author": "Sido Haakma",
  "license": "All rights reserved",
  "dependencies": {
    "nconf": "^0.8.4"
  }
}
