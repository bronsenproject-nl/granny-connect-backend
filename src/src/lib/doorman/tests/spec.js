'use strict'

describe("Suite 1", () => {
/*
  it("should prefix route with /baseroute", () => {
    const baseroute = 'v1';
    // mimic express app object and misuse it to test route loader
    const app = {
      use : function(route) {
        const prefixedBaseroute = '/'+baseroute+'/';
        const prefixBaserouteFoundAt = route.indexOf(prefixedBaseroute);
        expect(prefixBaserouteFoundAt).toBe(0);
        //console.log('*'+route+'*');
      }
    };

    require('..')(app, __dirname, baseroute, {});
  });
*/
});
